
/*

const carrossel = document.querySelector("#carrossel");
    let index = 0;

    function next() {
      index = (index + 1) % carrossel.children.length;
      carrossel.children[index].classList.add("ativo");
      carrossel.children[index - 1].classList.remove("ativo");
    }

    function previous() {
      index = (index - 1 + carrossel.children.length) % carrossel.children.length;
      carrossel.children[index].classList.add("ativo");
      carrossel.children[index + 1].classList.remove("ativo");
    }

    setInterval(next, 2000);

    document.querySelector("#carrossel .botao-proximo").addEventListener("click", next);
    document.querySelector("#carrossel .botao-anterior").addEventListener("click", previous);
    */